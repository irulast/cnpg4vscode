# actionlint config — declare the self-hosted runner labels our scale
# sets advertise so the `runner-label` rule stops flagging them as
# unknown.
#
# The pools themselves are defined out-of-tree in
# /home/adam/WebstormProjects/k8s-setup/infrastructure/mke/arc/. This
# project only consumes them by label per the contract in
# specs/002-github-actions-deploy/contracts/workflow-triggers.md.

self-hosted-runner:
  labels:
    - mke-default
    - mke-builds
